# 기여 안내

## 번역 수정

1. `translations/dead_space_ko.csv`에서 대상 문자열 ID를 찾는다.
2. `id`와 `english`는 바꾸지 않고 `translation`만 수정한다.
3. CSV를 UTF-8 BOM으로 저장한다.
4. 쉼표, 큰따옴표, 줄바꿈이 있는 셀은 표준 CSV 인용 규칙을 지킨다.
5. 게임에서 실제 문맥, 줄바꿈, 자막 영역과 글리프 표시를 확인한다.

PR 설명에는 문자열 ID, 기존 문장, 수정 문장과 등장 장면을 적는다. 같은 영어 원문이라도
ID가 다르면 문맥에 따라 별도로 번역할 수 있다.

## 코드 수정

- 지원 EXE의 예상 바이트 검증을 유지한다.
- 새로운 메모리 패치는 시그니처 검색 실패와 바이트 불일치 시 아무 것도 쓰지 않아야
  한다.
- DLL 로더 락 안에서 복잡한 초기화를 추가하지 않는다. 기존처럼 작업 스레드에서
  설치한다.
- 보더리스 커서 코드는 게임이 포그라운드일 때만 `ClipCursor`를 적용해야 한다.
- 새 옵션은 `Config.h/.cpp`, `DeadSpaceFixes.ini`, 문서를 함께 갱신한다.
- 경고 없이 Release Win32 빌드되는지 확인하고 `docs/TESTING.md`의 관련 항목을
  회귀 테스트한다.

## 저장소에 올리지 않는 파일

게임 실행 파일과 원본/수정 STR, 다른 언어 패치 파일, DLL/LIB/PDB/EXE, 로그, 덤프,
백업, 배포 ZIP은 커밋하지 않는다. 필요한 바이너리 의존성은 출처와 버전을 문서화하고
로컬에만 둔다.

## 라이선스 주의

제3자 디렉터리의 파일은 각 원 라이선스를 따른다. 프로젝트 고유 코드와 번역 데이터의
저장소 전체 라이선스가 정해지기 전까지는 외부 재사용 조건을 임의로 단정하지 않는다.
